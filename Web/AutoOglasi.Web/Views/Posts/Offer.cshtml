@using System.Globalization
@model AutoOglasi.Web.ViewModels.Posts.SinglePostViewModel

@{
    ViewData["Title"] = $"{Model.Car.Make} {Model.Car.Model}";
    ViewData["Description"] = Model.Car.Description;
    ViewData["Image"] = $"{Context.Request.Scheme}://{Context.Request.Host}{Model.Car.Images.FirstOrDefault()}";
}

<div class="container-xl py-4" style="background-color: #0b0c10; color: #c5c6c7;">
    
    <div class="luxury-hero-container mb-5">
        <div id="postCarousel" class="carousel slide carousel-fade" data-bs-ride="carousel">
            <div class="carousel-inner">
                @for (int i = 0; i < Model.Car.Images.Count; i++)
                {
                    var img = Model.Car.Images[i];
                    <div class="carousel-item @(i == 0 ? "active" : "")">
                        <img src="@img" class="d-block luxury-carousel-img" alt="Hypercar view" />
                    </div>
                }
            </div>
            <button class="carousel-control-prev" type="button" data-bs-target="#postCarousel" data-bs-slide="prev">
                <span class="carousel-control-prev-icon" aria-hidden="true"></span>
            </button>
            <button class="carousel-control-next" type="button" data-bs-target="#postCarousel" data-bs-slide="next">
                <span class="carousel-control-next-icon" aria-hidden="true"></span>
            </button>
        </div>
    </div>

    <div class="row justify-content-center mb-5">
        <div class="col-12 col-lg-10">
            <div class="row row-cols-5 g-2">
                @for (int i = 0; i < Model.Car.Images.Count; i++)
                {
                    <div class="col">
                        <img src="@Model.Car.Images[i]" 
                             class="w-100 luxury-thumb" 
                             data-bs-target="#postCarousel" 
                             data-bs-slide-to="@i" 
                             alt="Thumbnail" />
                    </div>
                }
            </div>
        </div>
    </div>

    <div class="row justify-content-center mb-5 text-center text-lg-start">
        <div class="col-12 col-lg-10 d-flex flex-column flex-lg-row justify-content-between align-items-center">
            <div>
                <span class="text-uppercase small text-warning text-opacity-75" style="letter-spacing: 4px;">Exclusive Collection</span>
                <h1 class="display-5 text-white fw-bold text-uppercase mt-1 mb-2" style="letter-spacing: 2px;">
                    @Model.Car.Make @Model.Car.Model
                </h1>
                <p class="text-muted small"><i class="fas fa-clock me-2"></i>Curated on @Model.PublishedOn</p>
            </div>
            <div class="mt-4 mt-lg-0 text-center text-lg-end">
                <span class="text-muted small text-uppercase d-block" style="letter-spacing: 2px;">Investment Value</span>
                <span class="display-5 fw-bold" style="color: #d4af37;">
                    €@Model.Car.Price.ToString("N0", CultureInfo.CurrentCulture)
                </span>
            </div>
        </div>
    </div>

    <div class="row justify-content-center g-4 mb-5">
        <div class="col-12 col-lg-10">
            <div class="row row-cols-2 row-cols-md-3 g-3">
                <div class="col">
                    <div class="p-4 h-100 border border-secondary border-opacity-10" style="background: #1f2833;">
                        <div class="text-uppercase text-muted small pb-1" style="letter-spacing: 2px; font-size: 0.75rem;">Year of Production</div>
                        <div class="fs-4 text-white fw-light">@Model.Car.Year</div>
                    </div>
                </div>
                <div class="col">
                    <div class="p-4 h-100 border border-secondary border-opacity-10" style="background: #1f2833;">
                        <div class="text-uppercase text-muted small pb-1" style="letter-spacing: 2px; font-size: 0.75rem;">Transmission</div>
                        <div class="fs-4 text-white fw-light">@Model.Car.TransmissionType</div>
                    </div>
                </div>
                <div class="col">
                    <div class="p-4 h-100 border border-secondary border-opacity-10" style="background: #1f2833;">
                        <div class="text-uppercase text-muted small pb-1" style="letter-spacing: 2px; font-size: 0.75rem;">Engine Performance</div>
                        <div class="fs-4 text-white fw-light">@Model.Car.Horsepower <span class="fs-6 text-muted">BHP</span></div>
                    </div>
                </div>
                <div class="col">
                    <div class="p-4 h-100 border border-secondary border-opacity-10" style="background: #1f2833;">
                        <div class="text-uppercase text-muted small pb-1" style="letter-spacing: 2px; font-size: 0.75rem;">Fuel & Drivetrain</div>
                        <div class="fs-4 text-white fw-light">@Model.Car.FuelType</div>
                    </div>
                </div>
                <div class="col">
                    <div class="p-4 h-100 border border-secondary border-opacity-10" style="background: #1f2833;">
                        <div class="text-uppercase text-muted small pb-1" style="letter-spacing: 2px; font-size: 0.75rem;">Location</div>
                        <div class="fs-4 text-white fw-light text-truncate">@Model.Car.LocationCity</div>
                    </div>
                </div>
                <div class="col">
                    <div class="p-4 h-100 border border-secondary border-opacity-10" style="background: #1f2833;">
                        <div class="text-uppercase text-muted small pb-1" style="letter-spacing: 2px; font-size: 0.75rem;">Status</div>
                        <div class="fs-4 fw-light" style="color: #d4af37;">Available</div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <div class="row justify-content-center g-5 mb-5">
        <div class="col-12 col-lg-6">
            <h3 class="text-white fw-light mb-4 pb-2 border-bottom border-secondary border-opacity-25" style="letter-spacing: 2px;">The Story</h3>
            <p class="lh-lg text-muted small" style="white-space: pre-line; font-size: 0.95rem; font-family: monospace;">
                @Model.Car.Description
            </p>
        </div>
        
        <div class="col-12 col-lg-4">
            <div class="p-4 border border-secondary border-opacity-25 bg-black text-center">
                <h4 class="text-white fw-light mb-2" style="letter-spacing: 2px;">Private Viewing</h4>
                <p class="text-muted small mb-4">Serious inquiries only. Schedule a private inspection.</p>
                
                <hr class="border-secondary opacity-25 mb-4" />

                <div class="mb-3">
                    <span class="text-uppercase text-muted d-block small pb-1" style="letter-spacing: 1px;">Managing Concierge</span>
                    <span class="fs-5 text-white fw-light">@Model.SellerName</span>
                </div>

                <div class="mb-4">
                    <span class="text-uppercase text-muted d-block small pb-1" style="letter-spacing: 1px;">Origin</span>
                    <span class="fs-6 text-muted">@Model.Car.LocationCity, @Model.Car.LocationCountry</span>
                </div>

                <a href="tel:@Model.SellerPhoneNumber" class="btn border text-uppercase py-3 w-100 fw-bold" style="color: #d4af37; border-color: #d4af37 !important; letter-spacing: 2px; font-size: 0.85rem;">
                    <i class="fas fa-phone-alt me-2"></i> Contact Agent
                </a>
            </div>
        </div>
    </div>

</div>

@section Scripts
{
    <script src="~/js/owlcarousel/owl.carousel.min.js" asp-append-version="true"></script>
    <script>
        $(document).ready(function () {
            $('.owl-carousel').owlCarousel({
                lazyLoad: true,
                loop: true,
                responsive: true
            });
        });
    </script>
}
